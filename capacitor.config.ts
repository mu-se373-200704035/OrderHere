import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'project-orderhere',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
